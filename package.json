{
  "name": "pitaya-lab-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "PITAYA LAB - Monorepo: Tienda online + Panel de Administración",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev:store\" \"npm run dev:admin\"",
    "dev:store": "npm -w store run dev",
    "dev:admin": "npm -w admin run dev",
    "build": "npm -w store run build && npm -w admin run build",
    "build:store": "npm -w store run build",
    "build:admin": "npm -w admin run build",
    "preview:store": "npm -w store run preview",
    "preview:admin": "npm -w admin run preview",
    "clean": "rm -rf apps/store/.nuxt apps/store/.output apps/admin/.nuxt apps/admin/.output node_modules"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "resend": "^6.17.2"
  }
}
